<h1>{{title}}</h1>
<div class="card-columns pt-5">
  {{#each places}}
  <!-- Card -->
  <div class="card promoting-card shadow-sm">
    <!-- Card content -->
    <div class="card-body d-flex flex-row">
      <!-- Avatar -->
      <img src="{{creator.picture}}" class="avatar-img" height="50px" width="50px" alt="avatar">
      <!-- Content -->
      <div>
        <!-- Title -->
        <h4 class="card-title font-weight-bold mb-2">{{ name }}</h4>
        <!-- Subtitle -->
        <!--DATE FORMATTING
        <script>
          Handlebars.registerHelper('readableDate' function (dateMilli) {
          var d = (new Date(dateMilli) + '').split(' ');
          d[2] = d[2] + ',';
          return [d[0], d[1], d[2], d[3]].join(' ');
          }
          var datemilli = Date.parse('Sun May 11,2014');
          console.log(taskDate(datemilli));
        </script>-->
        <p class="card-text"><svg class="bi bi-clock" width="1em" height="1em" viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
  <path fill-rule="evenodd" d="M8 15A7 7 0 108 1a7 7 0 000 14zm8-7A8 8 0 110 8a8 8 0 0116 0z" clip-rule="evenodd"/>
  <path fill-rule="evenodd" d="M7.5 3a.5.5 0 01.5.5v5.21l3.248 1.856a.5.5 0 01-.496.868l-3.5-2A.5.5 0 017 9V3.5a.5.5 0 01.5-.5z" clip-rule="evenodd"/>
</svg>  {{createdDate}}</p>
      </div>
    </div>
    <!-- Card image -->
    <div class="view overlay">
      <img class="card-img-top rounded-0" src="{{pictureUrl}}" alt="Card image cap">
      <a href="#!">
        <div class="mask rgba-white-slight"></div>
      </a>
    </div>
    <!-- Card content -->
    <div class="card-body">
      <div>
        <!-- Text -->
        <p>{{ description }}</p>
        <!-- Button -->
        <div>
          <div>
            <a class="btn btn-link stretched-link" href="/places/{{_id}}">Read More</a>
          </div>
        </div>
      </div>
    </div>
  </div>
  {{/each}}
</div>
<!-- Card -->

<!-- Old Format -->
<!--{{#each places}}
  <article>
    <hr>
    <h4>{{ name }}</h4>
    <div class="shadow p-3 mb-5 bg-white rounded">
      <img class="img-feed" src="{{pictureUrl}}" />
    </div>
    <p><strong>{{ creator.name }}</strong> - {{ description }}</p>
    <small>{{createdDate}}</small><br>
  </article>
{{/each}} -->
<!-- Old Format -->